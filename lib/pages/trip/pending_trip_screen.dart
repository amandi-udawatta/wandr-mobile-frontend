import 'package:flutter/material.dart';

class PendingTripScreen extends StatelessWidget {
  const PendingTripScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

