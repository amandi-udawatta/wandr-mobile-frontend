import 'package:flutter/material.dart';

class TripOngoingScreen extends StatelessWidget {
  const TripOngoingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}